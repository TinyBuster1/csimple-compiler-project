int main()
{
    if (x > 4 && a > 1 || a < 0)
        asd = sad;

    x = x > 1;
    return 1;
}
int main(int x)
{
    if (x > 4 && a > 1 || a < 0)
        asd = sad;

    x = x > 1;
    return 1;
}
int main(int y, char x)
{
    if (x > 4 && a > 1 || a < 0)
        asd = sad;

    x = x > 1;
    return 1;
}
